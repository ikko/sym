"""This module provides graph traversal capabilities for Symbol objects.

It allows for traversing the graph of interconnected Symbols in different modes (e.g., 'graph' or 'tree')
and provides methods for generating various representations of the graph, such as ASCII art.
"""
import warnings

# GraphTraversal class moved to symbol.py to resolve circular dependency.